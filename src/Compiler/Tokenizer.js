class Tokenizer {
  static tokenTypes = {
    T_INT: '\d+',
    T_TERMINATOR: ';'
  }
  static tokenize (source) {
    let cue = source
    // Iterate until all characters are consumed
  }
}

export default Tokenizer
